#!/bin/bash

# Check if PYCODE environment variable is set
if [ -z "$PYCODE" ]; then
    echo "Error: PYCODE environment variable is not set."
    exit 1
fi

# Run Python code from PYCODE environment variable
python3 -c "$PYCODE"

